import XCTest
// @testable import RAW_mnemonic

class RAWMnemonicTests: XCTestCase {
	// func testLength() {
	// 	let length1 = wordCount(bytes: 4)
	// 	XCTAssertEqual(length1, 3)
		
	// 	let length2 = wordCount(bytes:12)
	// 	XCTAssertEqual(length2, 9)
		
	// 	let length3 = wordCount(bytes: 16)
	// 	XCTAssertEqual(length3, 12)
	// }
    
    // func testEncode() throws {
    //     var data: [UInt8] = [0x12, 0x34, 0x56, 0x78]
    //     var words = UnsafeMutableBufferPointer<UInt16>.allocate(capacity: 6)
    //     try data.RAW_access {
	// 		try encode($0, into:words.baseAddress!)
	// 	}
        
    //     let expectedWords: [UInt16] = [0x123, 0x456, 0x780]
    //     XCTAssertEqual([UInt16](words), expectedWords)
    // }
}